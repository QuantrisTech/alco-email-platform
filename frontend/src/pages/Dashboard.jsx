export default function Dashboard() {
  return (
    <div className="p-8">
      <p className="text-darktext/50 text-sm">
        Dashboard stats cards, recent campaigns, and quick template shortcuts
        land here — built after Contacts, per the confirmed sequence.
      </p>
    </div>
  )
}
